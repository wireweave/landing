---
const codeExample = `page "Login" {
  card center {
    heading "Welcome Back"
    input "Email" email
    input "Password" password
    button "Sign In" primary
    link "Forgot password?"
  }
}`;
---

<section class="pt-32 pb-20 px-4 sm:px-6 lg:px-8">
  <div class="max-w-7xl mx-auto">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <div>
        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold tracking-tight text-gray-900">
          Wireframes from
          <span class="text-primary-600">simple code</span>
        </h1>
        <p class="mt-6 text-lg text-gray-600 leading-relaxed">
          Wireweave is a domain-specific language that lets AI assistants generate beautiful wireframes instantly.
          Write simple, readable code and get professional UI mockups.
        </p>
        <div class="mt-8 flex flex-wrap gap-4">
          <a
            href="https://playground.wireweave.org"
            class="inline-flex items-center px-6 py-3 text-base font-medium text-white bg-primary-600 hover:bg-primary-700 rounded-lg transition-colors"
          >
            Try Playground
            <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
          <a
            href="https://docs.wireweave.org"
            class="inline-flex items-center px-6 py-3 text-base font-medium text-gray-700 bg-gray-100 hover:bg-gray-200 rounded-lg transition-colors"
          >
            Documentation
          </a>
        </div>
      </div>

      <div class="relative">
        <div class="absolute inset-0 bg-gradient-to-r from-primary-100 to-primary-50 rounded-2xl transform rotate-1"></div>
        <div class="relative bg-gray-900 rounded-xl overflow-hidden shadow-2xl">
          <div class="flex items-center gap-2 px-4 py-3 bg-gray-800 border-b border-gray-700">
            <div class="w-3 h-3 rounded-full bg-red-500"></div>
            <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
            <div class="w-3 h-3 rounded-full bg-green-500"></div>
            <span class="ml-2 text-sm text-gray-400">login.wire</span>
          </div>
          <pre class="p-6 text-sm font-mono text-gray-300 overflow-x-auto"><code set:text={codeExample} /></pre>
        </div>
      </div>
    </div>
  </div>
</section>
